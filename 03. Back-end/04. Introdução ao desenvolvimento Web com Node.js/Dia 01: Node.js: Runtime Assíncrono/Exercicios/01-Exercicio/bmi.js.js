const calcular = 1;

const ola = () => {
    console.log("Olá mundo!");
}

const calcularIMC = (peso, altura) => {
    return peso / (altura * altura);
}

const calcularIMC2 = (peso, altura) => peso / (altura * altura);

const calcularIMC3 = (peso, altura) => {
    return peso / (altura * altura);
}

const calcularIMC4 = (peso, altura) => {
    return peso / (altura * altura);
}   

const calcularIMC5 = (peso, altura) => {
    return peso / (altura * altura);
}

const calcularIMC6 = (peso, altura) => {
    return peso / (altura * altura);
}

let 